# there are two identity operator they are
# is and is not

a = 5
b = 5
print(a is b)  # identity operator actually compares the memory location or id
# the id for a and b is same so it returns true.
print(id(a))
print(id(b))
